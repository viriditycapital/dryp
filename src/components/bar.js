/**
 * Where we keep the links to the pages
 */

import React from 'react';

class Bar extends React.Component {
  constructor (props) {
    super(props);
  }

  render () {
    return (
      <div className="bar">
      </div>
    );
  }
}

export default Bar;